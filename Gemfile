source "https://rubygems.org"

# GitHub Pages와 동일한 환경으로 로컬 미리보기를 하려면 이 gem 하나면 됩니다.
# 로컬 실행:  bundle install  &&  bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"   # Ruby 3+ 에서 로컬 서버 실행에 필요
